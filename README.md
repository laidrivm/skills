# skills

``` bash
chmod +x link.sh
./link.sh triage /Project/d2ass     # relative git-fiendly link of a single skill to a project
./link.sh triage global             # gobal link to ~/.claude/skills
./link.sh triage /Project/d2ass --unlink
```